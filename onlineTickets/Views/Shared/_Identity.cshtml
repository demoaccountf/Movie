@inject UserManager<ApplicationUser> UserManager;

@if (!User.Identity.IsAuthenticated)
{
    <a class="btn btn-outline-success my-2 my-sm-0" asp-controller="Account" asp-action="Login">
        <i class="bg-info bi-box-arrow-in-right"></i> Log in
    </a>




    <span style="padding: 5px;"></span>


    <a class="btn btn-outline-primary my-2 my-sm-0" asp-controller="Account" asp-action="Register">
        <i class="bi bi-person-plus"></i> Register
    </a>
}
else
{
    <!-- TODO: NEED TO FIX HERE. NAVBAR BUTTON DOES NOT WORK -->
    @*User profile section
    <div class="dropdown">
        <button class="btn btn-outline-success dropdown-toggle" type="button" id="profile-btn" data-toggle="dropdown"
                aria-expanded="false">
            <i class="bi bi-person-badge"></i> Hello @UserManager.GetUserName(User)
        </button>
        <ul class="navbar-nav dropdown-menu" aria-labelledby="profile-btn">
            <li class="nav-item dropdown">
                <a class="dropdown-item" asp-controller="Orders" asp-action="Index">
                    <i class="bi bi-list"></i> Orders
                </a>
            </li>

            @if (User.IsInRole("Admin"))
            {
                <li class="nav-item dropdown">
                    <a class="dropdown-item" asp-controller="Account" asp-action="Users">
                        <i class="bi bi-people"></i> Users
                    </a>
                </li>
            }


        </ul>




    <div class="dropdown">
        <button class="btn btn-outline-success dropdown-toggle" type="button" id="profile-btn" data-toggle="dropdown"
                aria-expanded="false">
            <i class="bi bi-person-badge"></i> Hello @UserManager.GetUserName(User)
        </button>
        <ul class="navbar-nav dropdown-menu" aria-labelledby="profile-btn">
            <li class="nav-item dropdown">
                <a class="dropdown-item" asp-controller="Orders" asp-action="Index">
                    <i class="bi bi-list"></i> Orders
                </a>
            </li>

            @if (User.IsInRole("Admin"))
            {
                <li class="nav-item dropdown">
                    <a class="dropdown-item" asp-controller="Account" asp-action="Users">
                        <i class="bi bi-people"></i> Users
                    </a>
                </li>
            }


        </ul>
          *@

          @*  

    <ul class="navbar-nav flex-grow-1">


        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="profile-btn" role="button" data-toggle="dropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="bi bi-person-badge"></i> Hello @UserManager.GetUserName(User)
            </a>

            <div class="dropdown-menu" aria-labelledby="profile-btn">
                <a class="dropdown-item" asp-controller="Orders" asp-action="Index"><i class="bi bi-camera-reels"></i> Orders</a>
            </div>

            @if (User.IsInRole("Admin"))
            {
                <div class=" dropdown-menu" aria-labelledby="profile-btn">
                    <a class="dropdown-item" asp-controller="Account" asp-action="Users">
                        <i class="bi bi-people"></i> Users
                    </a>
                </div>
            }
        </li>


    </ul>
    *@

    <ul class="navbar-nav flex-grow-1">
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="profile-btn" role="button" data-toggle="dropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                <i class="bi bi-person-badge"></i> Hello @UserManager.GetUserName(User)
            </a>
            <div class="dropdown-menu" aria-labelledby="profile-btn">
                <a class="dropdown-item" asp-controller="Orders" asp-action="Index"><i class="bi bi-camera-reels"></i> Orders</a>

                @if (User.IsInRole("Admin"))
                {
                    <a class="dropdown-item" asp-controller="Account" asp-action="Users">
                        <i class="bi bi-people"></i> Users
                    </a>
                }
            </div>
        </li>
    </ul>



    <span style="padding: 5px;"></span>

    <form asp-controller="Account" asp-action="Logout">
        <button class="btn btn-outline-danger my-2 my-sm-0">
            <i class="bg-info bi-box-arrow-in-right"></i> Log out
        </button>
    </form>
}



